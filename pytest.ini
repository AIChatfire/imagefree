[pytest]
testpaths = tests
python_files = test_*.py
# 🔴 --basetemp 必须显式给：沙箱的 mkdir shim 会让默认 tmp 路径的创建失败，
#    表现是所有用例一起报错（不是某一条失败）。
addopts = -q --tb=short --basetemp=/tmp/if_pytest_tmp
filterwarnings =
    ignore::DeprecationWarning
